{% set categories = cookieConsent.getCookieConsent().getCookieCategories()|json_decode() %}
{% set cookies = cookieConsent.getCookieConsent().getCookies()|json_decode %}
{# {% set var = cookieConsent.getCookieConsent().checkConsent() %} #}
<div class="cookie-settings-panel">
	<div class="cookie-settings-content">
		<button class="cookie-settings-close" data-action="close-settings">{{ include('_craft-cookies/_svg/close') }}</button>
		<div class="cookie-settings-header">
			<h3>Cookie Settings</h3>
		</div>
		<div class="cookie-category-wrapper">
			<div class="cookie-category">
				<div class="cookie-category-header">
					<div class="cookie-category-title">Essential Cookies</div>
					<label class="cookie-category-toggle disabled">
						<input type="checkbox" disabled="" data-category="essential">
						<span class="cookie-category-slider">
							<span class="cookie-icon check-icon">{{ include('_craft-cookies/_svg/checkmark') }}</span>
						</span>
					</label>
					<button class="category-dropdown-opener">
						<span>{{ include('_craft-cookies/_svg/caret-down') }}</span>
					</button>
				</div>
				<div class="cookie-category-description">Essential cookies are necessary for the website to function properly. They cannot be disabled.</div>
				<div class="category-services-list">
					<p>Amazon Web Service</p>
					<p>Youtube</p>
					<p>Facebook pixel</p>
					<p>Google</p>
				</div>
			</div>
			{% for key, item in cookies %}
				{% if key === '' %}
					{% set key = 'Uncategorized' %}
				{% endif %}
				<div class="cookie-category">
					<div class="cookie-category-header">
						<div class="cookie-category-title">{{ key }} Cookies</div>
						<label class="cookie-category-toggle">
							<input type="checkbox" data-category="{{ item.handle }}">
							<span class="cookie-category-slider">
								<span class="cookie-icon check-icon">{{ include('_craft-cookies/_svg/checkmark') }}</span>
								<span class="cookie-icon close-icon">{{ include('_craft-cookies/_svg/close') }}</span>
							</span>
						</label>
						<button class="category-dropdown-opener">
							<span>{{ include('_craft-cookies/_svg/caret-down') }}</span>
						</button>
					</div>
					<div class="cookie-category-description">{{ item.description }}</div>
					<div class="category-services-list">
						{% if item.vendors %}
							{% for vendor in item.vendors %}
								{{ vendor|raw }}
							{% endfor %}
						{% endif %}

					</div>
				</div>
			{% endfor %}
		</div>
		<div class="cookie-settings-footer">
			<button class="cookie-consent-button primary" data-action="save-preferences">Save Preferences</button>
		</div>
	</div>
	<div class="cookie-consent-footer">
		<p>Powered by <a href="#">ContentReactor</a></p>
	</div>
</div>
